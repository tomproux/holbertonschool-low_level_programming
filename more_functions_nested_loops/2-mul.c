#include "main.h"

/**
* mul- elle fait une multiplication entre deux int
* @a: permet de donner un premier int
* @b: permet de donner un deuxieme int
* Return: Always 0 (Success)
*/
int mul(int a; int b)
{
	return (a * b);
}
